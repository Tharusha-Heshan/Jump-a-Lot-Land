#ifndef CONSTANTS_H
#define CONSTANTS_H

const int WINDOW_WIDTH = 800;
const int WINDOW_HEIGHT = 600;
const int TILE_SIZE = 40;
const int ROWS = 15;
const int COLS = 20;

// This allows all files to know which keys are currently pressed
extern bool keyStates[256];

#endif
